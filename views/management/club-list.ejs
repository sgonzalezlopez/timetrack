<link rel="stylesheet" href="/bootstrap-table/bootstrap-table.min.css">
<div class="card">
	<div class="card-body">
		<h5 class="card-title">Club</h5>
		<div class="table-responsive">
			<table id="table" data-toggle="table" data-sortable="true" data-url="/api/club" class="table table-striped table-bordered clickable" detail-url="/management/club/">
				<thead>
					<tr>
						<th data-field="name" data-sortable="true">
							<%= __('COL_name') %>
						</th>
						<th data-field="shortName" data-sortable="true">
							<%= __('COL_shortName') %>
						</th>
						<th data-field="id" data-formatter="actionFormatter" data-align="center" data-events="actionEvents">
							<%= __('COL_actions') %>
						</th>
					</tr>
				</thead>
			</table>
		</div>
	</div>
</div>

<script>
	function actionFormatter(value, row, index) {
		return [
			'<a class="view" href="/management/club/' + row._id + '" title="">',
			'<i class="fa fa-eye"></i>',
			"</a> ",
			'<%-permissions.includes("D") ? `<a class="delete" href="javascript:void(0)" title=""><i class="fa fa-trash"></i></a>`: ``%>'
		].join("");
	}
</script>